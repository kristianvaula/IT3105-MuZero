# AbstractStateManager (ASM)
# – Converts sequences of real game states into an abstract state using the representation network.
# – Provides legal actions for an abstract state, returns a policy and evaluation using the prediction 
# network, and predicts next abstract states and rewards using the dynamics network